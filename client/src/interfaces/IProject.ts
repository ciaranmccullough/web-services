export interface IProjectData {
    header: string;
    subheader: string;
    buttonText: string;
}

export interface IProject {
    title: string;
    link: string;
    img: string;
    description: string;
}
